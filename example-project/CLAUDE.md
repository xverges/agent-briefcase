# Example Project

This is an example project-specific CLAUDE.md that demonstrates how briefcase layering works.

Files in this folder are synced only to a target repo whose directory name matches `example-project`.

This file overrides any `CLAUDE.md` from the `shared/` folder.
